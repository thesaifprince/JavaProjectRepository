<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>HMS Staff Portal</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        html, body {
            height: 100%;
            margin: 0;
            overflow: hidden;
            font-family: 'Segoe UI', sans-serif;
        }

        /* Carousel as background */
        #bgCarousel,
        #bgCarousel .carousel-inner,
        #bgCarousel .carousel-item {
            height: 100vh;
        }

        #bgCarousel img {
            object-fit: cover;
            width: 100%;
            height: 100vh;
        }

        /* Glassmorphism card */
        .glass-card {
            position: absolute;
            width: 320px;
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            background: rgba(255, 255, 255, 0.15);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.25);
            color: #fff;
            cursor: move;
        }

        .glass-header {
            padding: 12px 18px;
            border-bottom: 1px solid rgba(255,255,255,0.3);
            font-weight: 600;
            text-align: center;
        }

        .glass-body {
            padding: 18px;
        }

        .form-control {
            background: rgba(255,255,255,0.25);
            border: none;
            color: #fff;
        }

        .form-control::placeholder {
            color: #e0e0e0;
        }

        .btn-glass {
            background: rgba(255,255,255,0.25);
            border: 1px solid rgba(255,255,255,0.4);
            color: #fff;
        }

        .btn-glass:hover {
            background: rgba(255,255,255,0.4);
        }

        /* Initial positions */
        #cardRegister { top: 80px; left: 80px; }
        #cardLogin { top: 120px; right: 80px; }
        #cardNotice { bottom: 80px; left: 35%; }
    </style>
</head>
<body>

<!-- Background Carousel -->
<div id="bgCarousel" class="carousel slide carousel-fade" data-bs-ride="carousel" data-bs-interval="4000">
    <div class="carousel-inner">
        <!-- Replace images with YOUR images -->
        <div class="carousel-item active">
            <img src="staffPortal_image1.jpg" alt="Background 1">
        </div>
        <div class="carousel-item">
            <img src="staffPortal_image1.jpg" alt="Background 2">
        </div>
        <div class="carousel-item">
            <img src="staffPortal_image1.jpg" alt="Background 3">
        </div>
    </div>
</div>

<!-- Register Card -->
<div class="glass-card" id="cardRegister">
    <div class="glass-header">Register Yourself</div>
    <div class="glass-body">
        <input class="form-control mb-2" placeholder="Staff Name">
        <input class="form-control mb-2" placeholder="Staff ID">
        <input type="email" class="form-control mb-2" placeholder="Email">
        <input type="password" class="form-control mb-3" placeholder="Password">
        <button class="btn btn-glass w-100">Register</button>
    </div>
</div>

<!-- Login Card -->
<div class="glass-card" id="cardLogin">
    <div class="glass-header">Staff Login</div>
    <div class="glass-body">
        <input class="form-control mb-2" placeholder="Staff ID">
        <input type="password" class="form-control mb-3" placeholder="Password">
        <button class="btn btn-glass w-100">Login</button>
    </div>
</div>

<!-- Notices Card -->
<div class="glass-card" id="cardNotice">
    <div class="glass-header">Staff Notices</div>
    <div class="glass-body">
        <ul class="list-unstyled small">
            <li>• Meeting at 3:00 PM</li>
            <li>• Salary credited</li>
            <li>• Duty roster updated</li>
        </ul>
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<!-- Draggable Logic -->
<script>
    const cards = document.querySelectorAll('.glass-card');

    cards.forEach(card => {
        let isDragging = false;
        let offsetX, offsetY;

        card.addEventListener('mousedown', e => {
            isDragging = true;
            offsetX = e.clientX - card.offsetLeft;
            offsetY = e.clientY - card.offsetTop;
            card.style.zIndex = 1000;
        });

        document.addEventListener('mousemove', e => {
            if (!isDragging) return;
            card.style.left = (e.clientX - offsetX) + 'px';
            card.style.top = (e.clientY - offsetY) + 'px';
        });

        document.addEventListener('mouseup', () => {
            isDragging = false;
        });
    });
</script>

</body>
</html>
